from django.shortcuts import render

# Create your views here.
# This file can be used for other general API views if needed,
# or for views that don't belong to a specific submodule like 'users', 'recipes', etc.
